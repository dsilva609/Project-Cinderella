@model UI.Models.Condition

<script>
	c3.generate({
		bindto: '#@Model.ID',
		data: {
			columns: [
				['New', @Model.NumNew],
				['Used', @Model.NumUsed]
			],
			type: 'donut'
		},
		donut: {
			title: "@Model.Title"
		}
	});
</script>