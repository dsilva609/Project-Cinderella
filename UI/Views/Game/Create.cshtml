@model BusinessLogic.Models.Game
	<h2>@ViewBag.Title</h2>

	@using (Html.BeginForm(MVC.Game.Create(), FormMethod.Post))
	{
		Html.RenderPartial(MVC.Game.Views._GameFormPartial, Model);
	}
	@Html.ActionLink("Back to List", MVC.Game.Index())